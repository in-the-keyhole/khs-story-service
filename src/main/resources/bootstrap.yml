spring:
  application:
    name: stories
  cloud:
    config:
      fail-fast: false
      retry:
        initial-interval: 2000
        max-interval: 10000
        max-attempts: 10
        multiplier: 3